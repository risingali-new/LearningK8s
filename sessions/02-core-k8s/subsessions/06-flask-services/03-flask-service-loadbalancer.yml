apiVersion: v1
kind: Service
metadata:
  name: flask-web-loadbalancer
  namespace: app-core
spec:
  type: LoadBalancer
  selector:
    app: flask-web
    tier: application
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000

