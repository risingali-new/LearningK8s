apiVersion: v1
kind: Service
metadata:
  name: postgres-db
  namespace: case-db-connectivity
  labels:
    app.kubernetes.io/name: postgres-db
    training.k8s.io/session: "31"
    training.k8s.io/case-study: pod-cannot-connect-to-database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
